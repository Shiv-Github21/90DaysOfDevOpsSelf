The commands of git 

1. Git init 
-> It initialised an empty git repo in your local machine always remember u do git init inside a folder.

2. Git add <file_name> 
-> After doing git add filename ur paticular file moves to staging area from file system. 

3. git restore --staged <file_name>
-> doing this ur particular file moves back to file system from staging area.

4. git -commit -m "mssg"
->changes commmited!!
